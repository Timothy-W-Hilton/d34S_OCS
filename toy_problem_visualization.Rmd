---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.1'
      jupytext_version: 1.2.4
  kernelspec:
    display_name: d34Shv
    language: python
    name: d34shv
---

```{python}
import toy_problem as TP
import holoviews as hv
import geoviews as gv
import geoviews.feature as gf
```

```{python}
hv.extension('bokeh')
```

```{python}
draw_plots = False
```

```{python}
da_of, ocean_flux, ocean_flux_KG, anthro_conc, ocean_conc, plant_conc = TP.main()
```

```{python}
# ocean fluxes, Kettle grid
OFKG = gv.Dataset(ocean_flux_KG).to(gv.QuadMesh, kdims=['lon', 'lat']) * gf.coastline
# ocean fluxes, regridded to match Jim's GEOS-Chem run
OF = gv.Dataset(da_of).to(gv.QuadMesh, kdims=['lon', 'lat']) * gf.coastline
if draw_plots:
    OFKG + OF
```

```{python}
qm_anthro = gv.Dataset(anthro_conc['COS'].sel(lev=1)).to(gv.QuadMesh, kdims=['lon', 'lat'])
if draw_plots:
    qm_anthro
```
